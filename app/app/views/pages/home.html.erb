

<div class="jumbotron">
	<div class="text-center" >
		<h1>Welcome to LAN-Seeker!</h1>
		<h4>Doing our part to bring the gaming community together.</h4>
	</div>
</div>


<% if current_user %>
	<% if current_user.profile %>
		<%= link_to "edit profile", "#", class:'btn btn-primary btn-block' %>
		<br>
		<%= link_to "View profile", user_path(current_user), class:'btn btn-info btn-block' %>
	<% else %>
		<%= link_to "Create a Profile", new_user_profile_path(current_user), class:'btn btn-warning btn-block' %>
	<% end %>

	<!-- this code is JS for linking to redirected path -->
	<script type="text/javascript">window.location.href='#';</script>
<% else %>

	<div class="row">
		<% if user_signed_in? %>

		<% else %>
			<div class="col-md-6">
				<div class="well">
					<h1 class="text-center">Sign Up</h1>
					<p class="text-center">While you're here you mite as well sign up. I mean its completely free and everything <i class='fa fa-smile-o'></i></p>
					<%= link_to "Sign Up", new_user_registration_path, class:"btn btn-info btn-lg btn-block" %>
				</div>
			</div>
		<% end %>
		<div class="col-md-6">
			<div class="well">
				<h1 class="text-center">Donate!</h2>
				<p>Donations are how i plan on keeping this site alive. I don't expect to live off donations.... yet.</p>
				<%= link_to "Drop Cash Here", donate_path, class:"btn btn-success btn-lg btn-block" %>
			</div>
		</div>
	</div>
<% end %>